{
  "name": "koa-joi-router-swagger-docs",
  "version": "1.2.3",
  "description": "OpenAPI(aka Swagger) API docs generator for koa-joi-router.",
  "main": "index.js",
  "scripts": {
    "bump": "standard-version",
    "release": "git push --follow-tags origin master && npm publish --registry=https://registry.npmjs.org/",
    "test": "nyc --all --reporter=html --reporter=text mocha -r intelli-espower-loader"
  },
  "license": "MIT",
  "dependencies": {
    "joi-to-swagger": "github:alexandreL/joi-to-swagger",
    "lodash": "^4.17.20",
    "path-to-regexp": "^6.2.0",
    "swagger-parser": "^10.0.2"
  },
  "devDependencies": {
    "eslint": "^7.20.0",
    "intelli-espower-loader": "^1.0.1",
    "joi": "^17.3.0",
    "koa-joi-router": "^7.0.0",
    "mocha": "^8.3.0",
    "nyc": "^15.1.0",
    "power-assert": "^1.6.1",
    "standard-version": "^9.1.0"
  },
  "optionalDependencies": {
    "class-validator": "^0.12.2",
    "class-validator-jsonschema": "^2.1.1",
    "reflect-metadata": "^0.1.13"
  }
}
